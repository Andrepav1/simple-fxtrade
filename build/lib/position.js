// Generated by CoffeeScript 2.5.1
(function() {
  var omit, validate;

  ({omit, validate} = require('./utils'));

  // GET /accounts/:accountId/[positions[/:id]|openPositions]
  exports.positions = function(req = {}) {
    var id, open, route;
    ({id, open} = req);
    route = (function() {
      switch (false) {
        case id == null:
          return `accounts/${this.options.accountId}/positions/${id}`;
        case !open:
          return `accounts/${this.options.accountId}/openPositions`;
        default:
          return `accounts/${this.options.accountId}/positions`;
      }
    }).call(this);
    return this.request(req, route);
  };

  // PUT /accounts/:accountId/positions/:id/close
  exports.positions.close = function(req = {}) {
    validate(req, ['id']);
    return this('put').request({
      body: omit(req, 'id')
    }, `accounts/${this.options.accountId}/positions/${req.id}/close`);
  };

}).call(this);
