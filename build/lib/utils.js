// Generated by CoffeeScript 2.5.1
(function() {
  var indexOf = [].indexOf;

  exports.validate = function(req, required) {
    var invalid, keys, param;
    keys = Object.keys(req);
    invalid = (function() {
      var i, len, results;
      results = [];
      for (i = 0, len = required.length; i < len; i++) {
        param = required[i];
        if (indexOf.call(keys, param) < 0) {
          results.push(param);
        }
      }
      return results;
    })();
    if (invalid.length > 0) {
      throw new Error(`Required parameters missing: ${invalid.join(', ')}`);
    }
  };

  exports.assign = require('lodash/assign');

  exports.omit = function(object, key) {
    var result;
    result = exports.assign({}, object);
    delete result[key];
    return result;
  };

}).call(this);
